# SEO 修复记录（2026-09-06）

本次修改针对 GSC 导出与代码审计中能够确认的站内问题。2026-09-07 已按用户要求部署到 Cloudflare Pages 生产环境，未执行 GSC 提交。历史“软 404”“已发现未编入索引”等状态需要重新抓取才能验证，不能由本地测试直接判定已解决。

## 发布记录

- 项目：`freesavevideo-online`，分支：`main`，部署地址：<https://e7c1da98.freesavevideo1.pages.dev>。
- 使用本地已有的 2026-09-06 23:51 构建产物，未由代理运行生产构建。部署前确认 400 个 sitemap URL 均有静态产物，1543 段 JSON-LD 均可解析，且包含本次修复内容。
- 产物未包含次日删除未使用 CSS 选择器的清理；该差异不影响页面内容、功能或 SEO。
- 正式域名已核验：中英文下载目录、英文 Playlist 音频页、英文 YouTube 指南、FAQ、Learn 均返回 200、自引用 canonical 正确；JSON-LD 可解析，sitemap 包含新增入口。

## 已实施

- 修复 FAQ、Learn 目录的 JSON-LD 输出；使用公共安全序列化函数，防止正文中的 `<` 提前终止脚本。
- 首页及相关链接优先展示 YouTube、Playlist、Shorts；下载目录、指南目录和推荐链接统一按实际可用语言筛选，避免生成不存在的本地化地址。
- 开放已有完整文案的 `/en/download/youtube-playlist-to-mp3`，与中文版本组成双语页面；未批量开放其他语言。
- 翻译 11 种语言下载目录的标题、说明及主要导航；移除关键词标签。中英文目录增加单视频、Shorts、播放列表、音频和批量链接的任务选择说明。
- 补齐印尼语 YouTube 下载页文案及指南标题模板。
- 中英文 YouTube / Shorts 指南使用独立操作、设备保存及失败排查内容，不再直接复用下载页介绍和通用推广问答。
- 中英文下载目录加入 sitemap；FAQ / Guide 索引页的 hreflang 排除 noindex 语言版本。
- 移除所有下载详情页、指南详情页重复显示的全站更新日志；sitemap 不再把一个共用日期标成全部 SEO 内容的修改日期。已有文章自己的更新时间保留。
- 根路径的 Cloudflare 与 SvelteKit 跳转统一至无尾斜杠语言主页，保留查询参数，并禁止共享缓存语言跳转；非法语言 Cookie 不再使解析抛异常。

## 本地验证

- `pnpm -C web test:seo-contracts`：覆盖真实路由数据、11 种语言的目录/推荐链接、双语音频页、两套根路径跳转、FAQ/Learn JSON-LD 的 Svelte SSR 输出、目录 SSR、独立指南 SSR 与 sitemap。
- `pnpm -C web check`：类型和 Svelte 检查。
- `pnpm -C web i18n:check-encoding`：编码检查。
- 未运行 `test:seo`，因为该脚本会触发生产构建。新回归脚本不依赖构建、网络或生产服务。
- SSR 测试仅替换框架上下文与装饰性的支持平台条，不模拟实际媒体解析成功率，也不代表已验证 Cloudflare 生产构建产物。

## 部署后验证

按照项目现有流程由维护者执行生产构建和部署。发布后检查：

1. `/en/download`、`/zh/download` 和 `/en/download/youtube-playlist-to-mp3` 返回 200，canonical 指向自身；sitemap 包含这三个地址。
2. `/en/faq`、`/en/learn` 的 JSON-LD 可以解析；`/en/guide` 的 hreflang 不再指向 noindex 的语言目录。
3. 根域名带查询参数访问时只发生一次语言跳转，目标为无尾斜杠地址，并保留参数。
4. 在 GSC 对中英文 YouTube 主下载页、目录和英文 Playlist 音频页使用实时网址检查，确认可抓取后请求编入索引，并重新提交 sitemap。
5. 记录发布时间，随后按周看核心 URL 的最后抓取时间、索引状态，以及排除品牌词后的展示次数；先判断是否重新抓取，再判断搜索表现变化。

## 仍需独立推进

外链或站外文章不能通过仓库修改恢复。本次没有发布文章、联系站长或修改第三方内容。优先核实已有链接是否仍存在、是否指向合适的核心下载页；不要把补充目录站链接当作本次索引修复已经成功的证据。

即使技术检查通过，也不能保证进入索引或取得关键词排名。后续依据重新抓取后的 GSC 数据决定是否进一步完善内容、减少重复页面或调整推广页面，避免再次大范围改变语言收录策略。
